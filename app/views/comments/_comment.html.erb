<div class="mb-3">
  <p><%= comment.content %>
    <small class="text-muted">
      Posted <%= time_ago_in_words(comment.created_at) %> ago by <%= link_to comment.user.email, profile_path(comment.user) %>
    </small>
  </p>
</div>