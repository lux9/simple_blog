<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <!-- Website logo -->
    <a class="navbar-brand" href="/">
    <%= image_tag 'logo.png', alt: 'Website Logo', width: '30', height: '24' %>
      SimpleBlog
    </a>

    <!-- Toggler/collapsible Button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <!-- Conditionally render the login button or user options based on user login status -->
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "New Post", new_post_path, class: "nav-link", "data-bs-toggle": "modal", "data-bs-target": "#newPostModal", class: "btn btn-primary" %>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/user_profile">User Profile</a>
          </li>
          <li class="nav-item">
            <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class:"nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Login', new_user_session_path, class: 'btn btn-primary mb-3', class:"nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
